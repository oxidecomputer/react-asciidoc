// `:hardbreaks-option:` turns every newline within a block into a hard line
// break (`<br>`), document-wide — including inside list items.
const hardbreaksOption = `
= Document
:hardbreaks-option:

A first line
and a second line.

* item line one
  item line two
`

export default hardbreaksOption
